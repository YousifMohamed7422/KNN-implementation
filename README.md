# KNN-implementation
This project implements a K-Nearest Neighbors (KNN) classification model using the Iris dataset (iris.csv). The dataset contains 150 samples of three iris species: Iris setosa, Iris versicolor, and Iris virginica. Each sample includes four numerical features: sepal length, sepal width, petal length, and petal width .
